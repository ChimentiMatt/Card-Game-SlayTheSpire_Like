const PathScreen = ({nextEncounter}) => {
  
  return (
    <div className="absolute z-50 top-0 w-screen h-screen bg-blue-200">

        <button onClick={nextEncounter}>Exit (temp button)</button>
    </div>
  )
}

export default PathScreen